{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "useSmtp": {
      "type": "boolean",
      "description": "Should SMTP be used?",
      "default": false
    },
    "useConnectionUrl": {
      "type": "boolean",
      "description": "Do we use a connectionUrl to establish the service?",
      "default": false
    },
    "host": {
      "type": "string",
      "description": "A named service for SMTP. (Only needed if useSmtpConnectionUrl is false)",
      "default" : ""
    },
    "port": {
      "type": "number",
      "description": "Connection port",
      "default" : 587
    },
    "user": {
      "type": "string",
      "description": "SMTP username. (Only needed if useSmtpConnectionUrl is false)",
      "default" : ""
    },
    "pass": {
      "type": "string",
      "description": "SMTP password. (Only needed if useSmtpConnectionUrl is false)",
      "default" : ""
    },
    "from": {
      "type": "string",
      "description": "Address for sender. (Only needed if useSmtpConnectionUrl is false)",
      "default" : ""
    },
    "connectionUrl": {
      "type": "string",
      "description": "URL the server can be accessed from. (Only needed if useSmtpConnectionUrl is true)",
      "default" : ""
    }
  },
  "required": []
}




